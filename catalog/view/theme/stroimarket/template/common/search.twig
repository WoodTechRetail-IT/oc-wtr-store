<div id="search" class="input-group">
  <input type="text" name="search" value="{{ search }}" placeholder="{{ text_search }}" />
  <button type="button" class="search-btn btn" {% if not text_btn_search %}aria-label="{{ text_search }}"{% endif %}>
    {% if not text_btn_search %}
    <i class="fi fi-rr-search"></i>
    {% else %}
    <span class="text-search">{{ text_search3 }}</span>
    {% endif %}
  </button>
</div>